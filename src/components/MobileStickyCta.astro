---
import { site } from "../config/site";
---

<div class="sticky-book md:hidden">
  <a href="/book">
    Book a session
    <span aria-hidden="true">·</span>
    <span>{site.play.offer.active ? `September ${site.play.pricePerChild}` : site.play.pricePerChild}</span>
  </a>
</div>
